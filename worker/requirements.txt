temporalio
elasticsearch
httpx

# Unstructured — open-source document parsing (Apache-2.0)
# The partition() auto-detects file type and routes to the right parser
unstructured[all-docs]

# Tesseract bindings for OCR (used by unstructured for images/scanned PDFs)
pytesseract
Pillow