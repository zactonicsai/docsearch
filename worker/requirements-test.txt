# Core test dependencies
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-mock>=3.14

# Application dependencies (needed for imports)
httpx>=0.27
elasticsearch>=8.0
temporalio>=1.7

# Pin pydantic + pydantic-core together to avoid ImportError mismatch.
# If you already have these installed, run:  pip install --upgrade pydantic pydantic-core
pydantic>=2.10
pydantic-core>=2.27
